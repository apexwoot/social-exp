import preloader from '../../../../assets/images/loader.svg';

const Preloader = (props) => {
  return (
    <div>
      <img alt="preloadersvg" src={preloader} />
    </div>
  );
};

export default Preloader;
